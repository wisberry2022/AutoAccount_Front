import React, { useEffect, useRef, useState } from "react";
import { styled } from "styled-components";

import {
  GapFlex,
  HorizonFlex,
  VerticalFlex,
} from "./component/atoms/div/StyledFlex";

const App = (): React.ReactElement => {
  return <div>hihi</div>;
};

export default App;
